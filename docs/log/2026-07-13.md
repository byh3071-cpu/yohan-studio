# 2026-07-13 세션 로그

## 작업 내용
쇼룸 플렉시블 자리배치기 사례 페이지 2차 보강 + 히어로 정렬 정비. 전 과정 머지·배포·라이브 검증까지 완료.

1. **로컬 미표시 원인 해결**: 로컬이 옛 브랜치(`fix/vhk-blog-v251-captures`, master 대비 40커밋 뒤)에 체크아웃돼 쇼룸 사례가 안 보였음 → master 전환으로 해결. 코드 문제 아님.
2. **케이스 스터디 기술 깊이 보강 (PR #49)**: 시니어 면접관 관점 리뷰에서 "배치 로직 설명 부재" 구멍 발견 → 공개 저장소 소스 확인 후 04 결정 2건(Fisher-Yates 셔플+제약 사전 필터링 / 채팅 LLM 복붙→Cursor 전환·연간 결제 교훈) + 08 한계 1건(전역 상태·긴 함수 자각) 추가.
3. **히어로 정렬 정비 (PR #51, 스냅컨텍스트 독푸딩)**: 요약문 한 줄(keep-all+축약), 개요 카드 어절 단위 두줄 균형(keep-all+text-wrap balance), 데모 영상↔카드 그리드 좌우 라인 일치(960px 제한 제거). 1920px playwright 실렌더 검증.
4. **스냅컨텍스트 피드백 이슈**: 빈 메모 핀이 프롬프트에 포함돼 노이즈 → snapcontext#12 등록 (자동 제외/확인 단계/라벨링 3안).
5. **운영 정비**: `gh pr merge` 허용 룰을 `~/.claude/settings.json`에 추가 (머지 게이트 반복 차단, 사용자 명시 승인).
6. **세션 간 충돌 정리**: 다른 세션(네이버 파이프라인 PR #50)의 untracked 파일이 pull을 막음 → 백업 후 진행. 사후 검증에서 "내용 다름" 판정이 PowerShell 리다이렉트 UTF-16 인코딩 허상으로 확인돼 백업 삭제(내용 100% 동일).

## 변경 파일
- `src/content/showroom/flexible-seat-randomizer.mdx` — decisions 2건·limitations 1건 추가, summary 축약
- `src/components/showroom/ShowroomProjectDetail.tsx` — lead keep-all·폭 해제, heroImg 960px 제한 제거
- `src/components/showroom/CaseStudyDetails.tsx` — cardValue keep-all + text-wrap balance
- `docs/patterns/PAT-003-powershell-redirect-encoding.md` — 신규 패턴
- `~/.claude/settings.json` — gh pr merge 허용 룰 (레포 외부)

## 커밋/배포
- PR #49 `055d61d`, PR #51 `f5dd912` — 둘 다 squash 머지, Vercel 프로덕션 배포, 라이브(yohanstudio.co) 손검증 완료.

## 이슈/교훈
- **PS 리다이렉트 인코딩 오판 (PAT-003)**: `git show ... > file`을 PowerShell에서 하면 UTF-16으로 저장돼 파일 비교가 전부 "다름"으로 나옴. 파일 비교는 `git diff --no-index`로, 리다이렉트가 필요하면 `-Encoding utf8` 명시.
- 머지 권한 게이트는 PR 단위로 사용자 명시 문장을 요구 → allow 룰 등록으로 해소.
- 스냅컨텍스트 핀 주석 기반 수정 지시는 실전에서 유효 (핀 1개 메모로 정렬 의도 특정 성공).

## 다음 액션
1. GAS 매출·재고 사례 착수 (Phase 6 선정, 코드 회수 확인 조건부) — 기존 1순위 유지
2. 네이버 파이프라인 후속 (다른 세션 관할, master 커밋본 기준으로 이어가기)
3. snapcontext#12 처리 여부 결정 (노뚝이)

---

# 2026-07-13 세션 로그 (2차 — 아침, 네이버 발행 파이프라인 완주)

## 작업 내용
특강 후기 네이버 발행을 완주하면서 발행 파이프라인 자체를 반자동으로 승급시킨 세션.

1. **네이버 서식 실험 (B옵션 검증)**: SmartEditor에 HTML 클립보드 붙여넣기 → 서식 18/18 생존 (제목·굵게·기울임·밑줄·취소선·글자색·형광펜·크기·인용구·구분선·리스트·링크·제로폭 빈줄). PS 5.1 `Set-Clipboard -AsHtml`의 한글 전멸(ANSI) 발견 → CF_HTML UTF-8 수동 빌드 스크립트로 우회 (PAT-004).
2. **원문 보강 먼저 (PR #54)**: "네이버는 티저, 원문이 상세판" 퍼널 원칙 확립 — 노션 러닝 로그 3편을 MDX 특강별 섹션에 통합해 본문 약 2배. 머지→Vercel 배포→라이브 검증 후 발행 진행.
3. **네이버 발행 (수동 4회차)**: playwright로 에디터 자동화 — HTML 붙여넣기, 라이브 블로그 섹션 자동 캡처 2장, filechooser 인터셉트로 이미지 자동 삽입. 최종 편집·제목(SEO형 변경)·발행은 요한. blog.naver.com/yohan3071/224344971660
4. **콘텐츠 허브 백필**: DB가 사실상 비어있던 것 발견 → 네이버 발행 4건 전부 기록 (채널 "네이버" 옵션 신설, 기존 옵션 보존·손실 0 검증). 발행 이력은 m.blog API로 기계 역추적.
5. **콘텐츠 OS 회고 + 승급 (PR #55)**: 수동 4회 완주로 트립와이어 발동 → 즉시 회고. 고생 5건→검증된 해법 매칭, naver-convert 요구사항 6항 확정, 🟢수동→🟡반자동 승급.
6. **naver-convert 스킬 제작·머지 (yohan-cc-skills#28, yohan-core 0.3.5)**: CodeRabbit 지적 2건(설명 정렬·ps1 BOM) 반영. 미머지 브랜치의 채널 규칙 흡수.
7. **PR 스타일 정비**: "🤖 Generated with Claude Code" 푸터 전면 제거(#54·#55 소급), Co-authored-by는 유지 — 메모리 영구화.
8. **BOM 수정 (PR #56)**: 새벽 세션에서 PAT-003(콘솔 표시 문제)으로 오판했던 mojibake의 실제 원인이 BOM 없는 .ps1의 ANSI 파싱으로 판명 — 정정 기록.

## 변경 파일
- `src/content/blog/dev-lectures-two-days.mdx` — 특강별 섹션 상세 통합 (본문 2배)
- `docs/superpowers/specs/2026-07-13-content-os-retro-naver4.md` — 회고 (신규)
- `docs/patterns/PAT-004-cf-html-clipboard-utf8.md` — 신규 패턴
- `scripts/Set-ClipboardHtmlUtf8.ps1` — 신규 (+BOM 수정)
- yohan-cc-skills: `plugins/yohan-core/skills/naver-convert/` 신설, 0.3.5

## 커밋/배포
- yohan-studio PR #54·#55·#56, yohan-cc-skills PR #28 — 전부 머지·배포 완료. 네이버 라이브 확인.

## 이슈/교훈
- 프로세스 3회+ 손으로 돌린 뒤 자동화 — 병목이 데이터로 확인된 곳만 도구화 (회고 원칙 그대로 실행됨).
- PS 5.1 이중 함정: ①`Set-Clipboard -AsHtml` CF_HTML ANSI 인코딩(PAT-004) ②BOM 없는 .ps1 ANSI 파싱. 콘솔 mojibake를 보면 표시/파싱/데이터 3계층을 구분해 진단할 것.
- SmartEditor는 합성 paste 무시(isTrusted) — 자동화는 네이티브 키 입력만.
- Notion 닫힌집합 옵션 추가는 기존 옵션 전부 포함 재선언 + 직후 재fetch 검증으로 안전하게 완료 (첫 실전 적용).

## 다음 액션
1. B v1 — 노션 러닝 로그 사이트 렌더 (선행: Vercel NOTION_API_KEY 등록=요한)
2. GAS 매출·재고 카페 사례 (Phase 6 선정, clasp 코드 회수=요한 계정 필요)
3. naver-convert 5회차 실전 투입 → 도구 개선 데이터 수집
