# 2026-05-20 — Phase 2 AI'm OS Edition 출시

## 세션 컨텍스트

Phase 2 (블로그+SEO) 13/13 완료 후, AI'm OS 포지셔닝으로 4개 새 라우트 추가.
구현 계획: `docs/superpowers/plans/2026-05-20-{011~014}*.md`.
브랜치: `feat/aim-os-edition`.

## 011-P2 Showroom 출시 ✅

- **변경 파일**:
  - `src/data/showroomProjects.ts` (신규, 7 카테고리 × 6 프로젝트)
  - `src/components/showroom/{ProjectCard, CategoryFilter, ProjectGrid, FeaturedBanner}.tsx` (신규)
  - `src/app/showroom/page.tsx` (신규, Suspense 경계)
  - `src/components/layout/SiteHeaderNav.tsx` (쇼룸 entry 추가)
  - `src/app/sitemap.ts` (/showroom 등록, priority 0.9)
- **결과**: 빌드 성공, `/showroom` 라우트 등록, 카테고리 필터 URL 쿼리 동기화 (`?cat=Notion%20OS` 등) 동작 예정
- **다음**: 012-P2 (Diagnosis AI'm Scan)
- **검증 미실시 (사람 검증 필요)**:
  - 브라우저 `http://localhost:3050/showroom` 카테고리 필터 클릭 동작
  - 다크모드 토큰 정합
  - 모바일 (375px) 단일 컬럼 stack

## 012-P2 Diagnosis (AI'm Scan v0.1) 출시 ✅

- **변경 파일**:
  - `src/data/aimScanQuestions.ts` (신규, 7영역 × 21문항)
  - `src/lib/aimScan.ts` (신규, 순수 점수 함수)
  - `src/components/diagnosis/{LevelBadge, ScoreRadar, ResultPanel, DiagnosisForm}.tsx` (신규)
  - `src/app/diagnosis/page.tsx` (신규)
  - `src/components/layout/SiteHeaderNav.tsx` (진단 entry)
  - `src/app/sitemap.ts` (/diagnosis 등록, priority 0.8)
  - `package.json` + lockfile (tsx dev dep — 점수 sanity 검증용)
- **결과**: 빌드 성공, `/diagnosis` 200, ScoreRadar는 외부 차트 라이브러리 없이 SVG 직접 그림 (7축, 4단계 그리드)
- **점수 sanity 검증**: 3 케이스 통과 — 전 5점→100%·Lv5, 전 0점→0%·Lv1, Direction만 만점→top3에 미포함
- **블로커 발생/해소**: 세션 중 브랜치 reorg 두 번 발생 (`docs/content-prep`, `docs/track-b-content-only`). ResultPanel commit 2개 cherry-pick으로 `feat/aim-os-edition`에 복귀
- **검증 미실시 (사람 검증 필요)**:
  - 브라우저 21문항 라디오 선택 + 진행률 sticky bar
  - 결과 패널 (총점/레벨/레이더/TOP3/추천순서) 렌더
  - "다시 진단" 리셋 + 다크모드 토큰 정합

## 013-P2 Services 출시 ✅

- **변경 파일**:
  - `src/data/services.ts` (신규, 3 서비스 + AggregateOffer 가격 범위)
  - `src/components/services/{ServiceCard, PricingSection}.tsx` (신규)
  - `src/app/services/page.tsx` (신규 + Service JSON-LD `@graph`)
  - `src/components/layout/SiteHeaderNav.tsx` (서비스 entry)
  - `src/app/sitemap.ts` (/services 등록, priority 0.9)
- **결과**: 빌드 성공, `/services` route 등록, JSON-LD 3개 Service (Scan Report / OS Template / OS Build) + AggregateOffer (KRW lowPrice/highPrice)
- **env 변수**: `NEXT_PUBLIC_CONTACT_FORM_URL` — 사용자가 `.env.local`에 외부 폼 URL 채워야 CTA 동작
- **`.env.example` 미커밋**: 프로젝트 `.gitignore`에 `.env*` 패턴 — 환경 변수는 별도 핸드오프 문서로 전달 필요
- **검증 미실시 (사람 검증 필요)**:
  - 브라우저 카드 3개 + ★ POPULAR 배지 (OS Template)
  - CTA 클릭 → 외부 폼 `?service=<slug>` prefill
  - JSON-LD 파싱 (Schema.org Validator)

## 014-P2 Home 리뉴얼 출시 ✅

- **변경 파일**:
  - `src/components/sections/Hero.tsx` (카피·CTA 변경 — AI'm OS 포지셔닝, /diagnosis + /services CTA)
  - `src/components/sections/{Philosophy, ProblemSection, ShowroomPreview, ScanIntro, ServicesPreview}.tsx` (신규 5개)
  - `src/app/page.tsx` (Portfolio + Featured 제거, 6 신규 섹션 + About/Faq/Contact 보존)
- **결과**: 빌드 14/14 페이지, 홈 9 섹션 구성: Hero → Philosophy → Problem → ShowroomPreview → ScanIntro → ServicesPreview → About → Faq → Contact
- **보존**: 기존 Portfolio.tsx / Featured.tsx 파일은 디스크에 남김 (라우트 /portfolio에서 계속 사용 중)
- **검증 미실시 (사람 검증 필요)**:
  - 9 섹션 순서 + 다크모드 토큰 정합
  - Hero CTA 클릭 → /diagnosis, /services 이동
  - ShowroomPreview 카드 3개 (featured 우선)
  - ScanIntro 7/21/5 통계 표시
  - 모바일 (375px) 모든 grid 단일 컬럼 stack

---

## Phase 2 AI'm OS Edition 총 출시 요약

- **4 plans, 32 implementer tasks 완료** (`feat/aim-os-edition`)
- **신규 라우트 3개**: /showroom, /diagnosis, /services
- **신규 데이터**: showroomProjects (6), aimScanQuestions (21문항), services (3)
- **신규 컴포넌트**: 5 showroom + 4 diagnosis + 2 services + 5 home sections = 16개
- **순수 함수 로직**: aimScan.ts (점수 계산, 레벨 판정, TOP3, 추천 순서) — sanity 3 케이스 통과
- **외부 라이브러리 추가**: tsx (dev only)
- **세션 내 이슈**:
  - 브랜치 두 번 reorg (`docs/content-prep` → `docs/track-b-content-only`) — cherry-pick으로 복귀
  - .env.example gitignore — 환경 변수 별도 핸드오프 필요
- **남은 작업**:
  - 사람 손검증 (전 라우트 200, 다크모드, 모바일)
  - Plan 013 NEXT_PUBLIC_CONTACT_FORM_URL 실 폼 URL 주입
  - Phase 3 진입 (Supabase + Stripe)
