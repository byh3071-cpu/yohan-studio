# 2026-06-15 세션 로그

## 작업 내용
요한 스튜디오 SEO 점검 → 라이브 실측 검증 → SEO 반복 개선(P1~P3) 3라운드.
각 라운드마다 **순서 작업 → 자가 피드백 루프 → 적대적 검증(실서버 렌더 HTML/PNG 실측) → 코드 리뷰** 적용.

### 1. SEO 현황 점검 + 라이브 검증
- 코드 SEO 감사: 메타데이터·OG·sitemap·robots·JSON-LD(+FAQPage)·GA·Google/Naver 인증·301 모두 구현 확인.
- 라이브 실측(Vercel MCP `web_fetch_vercel_url`, WebFetch는 403): robots.txt·sitemap 정상 작동 확인.
- **발견**: 인증값은 layout.tsx 하드코딩 폴백 존재 → env 없어도 출력. 블로커(env 미설정)는 실측상 해소.

### 2. SEO 반복 개선 (P1~P3)
- **P1**: 홈 전용 keyword-rich 메타데이터(title.absolute·description·canonical·OG·twitter).
- **P2**: `PageFaq` 재사용 컴포넌트 → `/services`·`/diagnosis` FAQPage 스키마 + 가시 섹션. 페이지별 전용 OG 이미지.
- **P2(b)**: `/services`·`/diagnosis` BreadcrumbList JSON-LD.
- **P3**: 공유 `ogCard` 헬퍼로 OG 카드 중복 ~5배 제거 + `/blog`·`/showroom`·`/design`·`/open-source` 전용 OG 카드.
- sitemap: 정적 페이지 `lastModified`를 빌드시각(`new Date()`) → 안정 상수로 변경(freshness 신호 노이즈 제거).

### 3. canonical 도메인 정합 (P0)
- 적대적 검증 중 발견: 코드 기본값 `yohanstudio.co` vs 라이브 robots/sitemap `yohan-studio.vercel.app` 불일치.
- `next.config.ts`에 호스트 기반 308 리다이렉트(`yohan-studio.vercel.app/*` → `https://yohanstudio.co/*`) 추가 — 중복 콘텐츠 차단.

### 적대적 검증으로 잡은 회귀
- **홈 OG 회귀**: Next.js openGraph/twitter 얕은-교체 특성 탓에 홈 메타 override가 layout의 `og:site_name`·`og:locale`·`twitter:card=summary_large_image`를 소실시킴 → 복원 수정.
- vhk OG dead code 자가 교정, alt 감사(폴백 건강) 등.

## 변경 파일
- `next.config.ts` (호스트 301)
- `src/app/page.tsx` (홈 메타)
- `src/app/{services,diagnosis}/page.tsx` (FAQ·Breadcrumb·canonical·title)
- `src/components/seo/PageFaq.tsx`, `src/components/seo/ogCard.tsx` (신규)
- `src/app/opengraph-image.tsx` + `{blog,showroom,design,open-source,services,diagnosis}/opengraph-image.tsx`
- `src/app/sitemap.ts`
- `CLAUDE.md`

## 커밋 (391821f → 89fbafc)
- `89fbafc` per-page OG cards via shared helper
- `a7ebb06` canonical host redirect + breadcrumbs
- `2cdc61e` fix home og:siteName/locale + large twitter card
- `1a026e0` OG badge URL derive + title de-dup
- `3459914` home metadata + page FAQ + per-page OG + sitemap lastmod
- `46d683d` CLAUDE.md status update

## 검증
- `npm run build` 클린(0경고), `npm run lint` 0에러(경고는 무관 스크립트), 전 페이지 렌더 HTML/OG PNG 실측 통과.

## 다음 액션
1. **(사람)** Vercel `NEXT_PUBLIC_SITE_URL=https://yohanstudio.co` 설정 + 도메인 연결 확인 → 재배포.
2. 재배포 후 라이브에서 canonical·sitemap·OG 카드 손검증.
3. (선택) 스토어 라이브 시 `/store/[id]` 상품 동적 OG.

## 이슈/블로커
- 프로덕션 env `NEXT_PUBLIC_SITE_URL`이 vercel.app로 남아있으면 canonical 태그가 308 리다이렉트와 충돌 → #1 액션 필수.
- hreflang: 단일 ko-KR이라 won't-do 권장.
